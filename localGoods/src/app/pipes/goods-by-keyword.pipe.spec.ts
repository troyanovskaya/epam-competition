import { GoodsByKeywordPipe } from './goods-by-keyword.pipe';

describe('GoodsByKeywordPipe', () => {
  it('create an instance', () => {
    const pipe = new GoodsByKeywordPipe();
    expect(pipe).toBeTruthy();
  });
});
